# Cloudflare Web Analytics token (free, privacy-friendly).
# Get one at https://dash.cloudflare.com/?to=/:account/web-analytics by adding
# this site. When this var is unset, no analytics script is loaded — so dev
# and preview builds never ping the prod beacon.
VITE_CF_ANALYTICS_TOKEN=
